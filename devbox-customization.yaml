tasks:
  - name: Install VSCode
    type: winget
    parameters:
      id: Microsoft.VisualStudioCode

  - name: Install Teams Toolkit Extension
    type: powershell
    parameters:
      script: |
        code --install-extension ms-teams.vscode-teams
